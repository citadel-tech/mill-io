[package]
name = "mill-rpc-core"
version = "0.1.0"
edition = "2021"
description = "Core types, wire protocol, and codec traits for Mill-RPC"

[dependencies]
serde = { version = "1", features = ["derive"] }
bincode = "1"
thiserror = "2"
